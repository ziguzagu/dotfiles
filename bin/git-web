#!/bin/bash
set -eu

branch="$(git rev-parse --abbrev-ref HEAD)"
default_branch="$(git symbolic-ref --short HEAD)"

if [ $branch = 'master' ] || [ $branch = $default_branch ]; then
  hub browse
else
  hub browse -- pull/$branch
fi
