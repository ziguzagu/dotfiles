#!/bin/bash
set -eu

branch="$(git rev-parse --abbrev-ref HEAD)"

# TODO: get default branch and compares it and 'master'
if [ $branch = 'master' ] || [ $branch = 'develop' ]; then
  hub browse
else
  hub browse -- pull/$branch
fi
